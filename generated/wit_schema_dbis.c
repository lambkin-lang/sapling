/* Auto-generated by tools/wit_codegen; DO NOT EDIT. */
#include "generated/wit_schema_dbis.h"
#include <string.h>

#define SAP_WIT_CHECK(rc) do { if ((rc) != ERR_OK) return (rc); } while (0)

const SapWitDbiSchema sap_wit_dbi_schema[] = {
    {0u, "app_state", "dbi0-app-state-key", "dbi0-app-state-value"},
    {1u, "inbox", "dbi1-inbox-key", "dbi1-inbox-value"},
    {2u, "outbox", "dbi2-outbox-key", "dbi2-outbox-value"},
    {3u, "leases", "dbi3-leases-key", "dbi3-leases-value"},
    {4u, "timers", "dbi4-timers-key", "dbi4-timers-value"},
    {5u, "dedupe", "dbi5-dedupe-key", "dbi5-dedupe-value"},
    {6u, "dead_letter", "dbi6-dead-letter-key", "dbi6-dead-letter-value"},
};

const uint32_t sap_wit_dbi_schema_count =
    (uint32_t)(sizeof(sap_wit_dbi_schema) / sizeof(sap_wit_dbi_schema[0]));

/* ---- Writer functions ---- */

int sap_wit_write_message_envelope(ThatchRegion *region, const SapWitMessageEnvelope *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
    SAP_WIT_CHECK(thatch_write_data(region, &val->message_id_len, 4));
    SAP_WIT_CHECK(thatch_write_data(region, val->message_id_data, val->message_id_len));
    if (val->has_from_worker) {
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_OPTION_SOME));
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
        SAP_WIT_CHECK(thatch_write_data(region, &val->from_worker, 8));
    } else {
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_OPTION_NONE));
    }
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->to, 8));
    {
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_TUPLE));
        uint32_t _tuple_skip_loc;
        SAP_WIT_CHECK(thatch_reserve_skip(region, &_tuple_skip_loc));
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
        SAP_WIT_CHECK(thatch_write_data(region, &val->route_0, 8));
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
        SAP_WIT_CHECK(thatch_write_data(region, &val->route_1, 8));
        SAP_WIT_CHECK(thatch_commit_skip(region, _tuple_skip_loc));
    }
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_ENUM));
    SAP_WIT_CHECK(thatch_write_data(region, &val->kind, 1));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_FLAGS));
    SAP_WIT_CHECK(thatch_write_data(region, &val->message_flags, 4));
    SAP_WIT_CHECK(thatch_write_tag(region, val->requires_ack ? SAP_WIT_TAG_BOOL_TRUE : SAP_WIT_TAG_BOOL_FALSE));
    if (val->has_trace_id) {
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_OPTION_SOME));
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
        SAP_WIT_CHECK(thatch_write_data(region, &val->trace_id_len, 4));
        SAP_WIT_CHECK(thatch_write_data(region, val->trace_id_data, val->trace_id_len));
    } else {
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_OPTION_NONE));
    }
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
    SAP_WIT_CHECK(thatch_write_data(region, &val->payload_len, 4));
    SAP_WIT_CHECK(thatch_write_data(region, val->payload_data, val->payload_len));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_lease_info(ThatchRegion *region, const SapWitLeaseInfo *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->owner, 8));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->deadline_ts, 8));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->attempts, 8));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_message_ack(ThatchRegion *region, const SapWitMessageAck *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
    SAP_WIT_CHECK(thatch_write_data(region, &val->message_id_len, 4));
    SAP_WIT_CHECK(thatch_write_data(region, val->message_id_data, val->message_id_len));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->committed_at, 8));
    SAP_WIT_CHECK(thatch_write_tag(region, val->accepted ? SAP_WIT_TAG_BOOL_TRUE : SAP_WIT_TAG_BOOL_FALSE));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi0_app_state_key(ThatchRegion *region, const SapWitDbi0AppStateKey *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
    SAP_WIT_CHECK(thatch_write_data(region, &val->namespace_len, 4));
    SAP_WIT_CHECK(thatch_write_data(region, val->namespace_data, val->namespace_len));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
    SAP_WIT_CHECK(thatch_write_data(region, &val->key_len, 4));
    SAP_WIT_CHECK(thatch_write_data(region, val->key_data, val->key_len));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi0_app_state_value(ThatchRegion *region, const SapWitDbi0AppStateValue *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
    SAP_WIT_CHECK(thatch_write_data(region, &val->body_len, 4));
    SAP_WIT_CHECK(thatch_write_data(region, val->body_data, val->body_len));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->revision, 8));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->updated_at, 8));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_F64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->confidence, 8));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi1_inbox_key(ThatchRegion *region, const SapWitDbi1InboxKey *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->worker, 8));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->seq, 8));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi1_inbox_value(ThatchRegion *region, const SapWitDbi1InboxValue *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(sap_wit_write_message_envelope(region, &val->envelope));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi2_outbox_key(ThatchRegion *region, const SapWitDbi2OutboxKey *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->seq, 8));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi2_outbox_value(ThatchRegion *region, const SapWitDbi2OutboxValue *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(sap_wit_write_message_envelope(region, &val->envelope));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->committed_at, 8));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi3_leases_key(ThatchRegion *region, const SapWitDbi3LeasesKey *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
    SAP_WIT_CHECK(thatch_write_data(region, &val->message_id_len, 4));
    SAP_WIT_CHECK(thatch_write_data(region, val->message_id_data, val->message_id_len));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi4_timers_key(ThatchRegion *region, const SapWitDbi4TimersKey *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->due_ts, 8));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
    SAP_WIT_CHECK(thatch_write_data(region, &val->message_id_len, 4));
    SAP_WIT_CHECK(thatch_write_data(region, val->message_id_data, val->message_id_len));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi4_timers_value(ThatchRegion *region, const SapWitDbi4TimersValue *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(sap_wit_write_message_envelope(region, &val->envelope));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi5_dedupe_key(ThatchRegion *region, const SapWitDbi5DedupeKey *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
    SAP_WIT_CHECK(thatch_write_data(region, &val->message_id_len, 4));
    SAP_WIT_CHECK(thatch_write_data(region, val->message_id_data, val->message_id_len));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi5_dedupe_value(ThatchRegion *region, const SapWitDbi5DedupeValue *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, val->accepted ? SAP_WIT_TAG_BOOL_TRUE : SAP_WIT_TAG_BOOL_FALSE));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->last_seen_ts, 8));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
    SAP_WIT_CHECK(thatch_write_data(region, &val->checksum_len, 4));
    SAP_WIT_CHECK(thatch_write_data(region, val->checksum_data, val->checksum_len));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi6_dead_letter_key(ThatchRegion *region, const SapWitDbi6DeadLetterKey *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->worker, 8));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->seq, 8));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi6_dead_letter_value(ThatchRegion *region, const SapWitDbi6DeadLetterValue *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(sap_wit_write_message_envelope(region, &val->envelope));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->failure_code, 8));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->attempts, 8));
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_S64));
    SAP_WIT_CHECK(thatch_write_data(region, &val->failed_at, 8));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_lease_state(ThatchRegion *region, const SapWitLeaseState *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_VARIANT));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_data(region, &val->case_tag, 1));
    switch (val->case_tag) {
    case SAP_WIT_LEASE_STATE_QUEUED:
        break;
    case SAP_WIT_LEASE_STATE_LEASED:
        SAP_WIT_CHECK(sap_wit_write_lease_info(region, &val->val.leased));
        break;
    case SAP_WIT_LEASE_STATE_DONE:
        break;
    case SAP_WIT_LEASE_STATE_FAILED:
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
        SAP_WIT_CHECK(thatch_write_data(region, &val->val.failed.len, 4));
        SAP_WIT_CHECK(thatch_write_data(region, val->val.failed.data, val->val.failed.len));
        break;
    default: return ERR_TYPE;
    }
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_dbi3_leases_value(ThatchRegion *region, const SapWitDbi3LeasesValue *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_RECORD));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(sap_wit_write_lease_state(region, &val->state));
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

int sap_wit_write_tx_error(ThatchRegion *region, const SapWitTxError *val)
{
    SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_VARIANT));
    ThatchCursor skip_loc;
    SAP_WIT_CHECK(thatch_reserve_skip(region, &skip_loc));
    SAP_WIT_CHECK(thatch_write_data(region, &val->case_tag, 1));
    switch (val->case_tag) {
    case SAP_WIT_TX_ERROR_BUSY:
        break;
    case SAP_WIT_TX_ERROR_CONFLICT:
        break;
    case SAP_WIT_TX_ERROR_NOT_FOUND:
        break;
    case SAP_WIT_TX_ERROR_SCHEMA_MISMATCH:
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
        SAP_WIT_CHECK(thatch_write_data(region, &val->val.schema_mismatch.len, 4));
        SAP_WIT_CHECK(thatch_write_data(region, val->val.schema_mismatch.data, val->val.schema_mismatch.len));
        break;
    case SAP_WIT_TX_ERROR_INTERNAL:
        SAP_WIT_CHECK(thatch_write_tag(region, SAP_WIT_TAG_BYTES));
        SAP_WIT_CHECK(thatch_write_data(region, &val->val.internal.len, 4));
        SAP_WIT_CHECK(thatch_write_data(region, val->val.internal.data, val->val.internal.len));
        break;
    default: return ERR_TYPE;
    }
    SAP_WIT_CHECK(thatch_commit_skip(region, skip_loc));
    return ERR_OK;
}

/* ---- Reader functions ---- */

int sap_wit_read_message_envelope(const ThatchRegion *region, ThatchCursor *cursor, SapWitMessageEnvelope *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->message_id_len));
    { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->message_id_len, &p));
      out->message_id_data = (const uint8_t *)p; }
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag == SAP_WIT_TAG_OPTION_SOME) {
        out->has_from_worker = 1;
        { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
          if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
        SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->from_worker));
      } else if (tag == SAP_WIT_TAG_OPTION_NONE) {
        out->has_from_worker = 0;
      } else return ERR_TYPE; }
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->to));
    {
        { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
          if (tag != SAP_WIT_TAG_TUPLE) return ERR_TYPE; }
        uint32_t _skip_len;
        SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
        uint32_t _remaining = thatch_region_used(region) - *cursor;
        if (_skip_len > _remaining) return ERR_RANGE;
        ThatchCursor _segment_end = *cursor + _skip_len;
        { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
          if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
        SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->route_0));
        { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
          if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
        SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->route_1));
        if (*cursor != _segment_end) return ERR_TYPE;
    }
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_ENUM) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 1, &out->kind));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_FLAGS) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->message_flags));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag == SAP_WIT_TAG_BOOL_TRUE) out->requires_ack = 1;
      else if (tag == SAP_WIT_TAG_BOOL_FALSE) out->requires_ack = 0;
      else return ERR_TYPE; }
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag == SAP_WIT_TAG_OPTION_SOME) {
        out->has_trace_id = 1;
        { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
          if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
        SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->trace_id_len));
        { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->trace_id_len, &p));
          out->trace_id_data = (const uint8_t *)p; }
      } else if (tag == SAP_WIT_TAG_OPTION_NONE) {
        out->has_trace_id = 0;
      } else return ERR_TYPE; }
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->payload_len));
    { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->payload_len, &p));
      out->payload_data = (const uint8_t *)p; }
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_lease_info(const ThatchRegion *region, ThatchCursor *cursor, SapWitLeaseInfo *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->owner));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->deadline_ts));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->attempts));
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_message_ack(const ThatchRegion *region, ThatchCursor *cursor, SapWitMessageAck *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->message_id_len));
    { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->message_id_len, &p));
      out->message_id_data = (const uint8_t *)p; }
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->committed_at));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag == SAP_WIT_TAG_BOOL_TRUE) out->accepted = 1;
      else if (tag == SAP_WIT_TAG_BOOL_FALSE) out->accepted = 0;
      else return ERR_TYPE; }
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi0_app_state_key(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi0AppStateKey *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->namespace_len));
    { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->namespace_len, &p));
      out->namespace_data = (const uint8_t *)p; }
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->key_len));
    { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->key_len, &p));
      out->key_data = (const uint8_t *)p; }
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi0_app_state_value(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi0AppStateValue *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->body_len));
    { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->body_len, &p));
      out->body_data = (const uint8_t *)p; }
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->revision));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->updated_at));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_F64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->confidence));
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi1_inbox_key(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi1InboxKey *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->worker));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->seq));
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi1_inbox_value(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi1InboxValue *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    SAP_WIT_CHECK(sap_wit_read_message_envelope(region, cursor, &out->envelope));
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi2_outbox_key(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi2OutboxKey *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->seq));
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi2_outbox_value(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi2OutboxValue *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    SAP_WIT_CHECK(sap_wit_read_message_envelope(region, cursor, &out->envelope));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->committed_at));
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi3_leases_key(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi3LeasesKey *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->message_id_len));
    { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->message_id_len, &p));
      out->message_id_data = (const uint8_t *)p; }
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi4_timers_key(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi4TimersKey *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->due_ts));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->message_id_len));
    { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->message_id_len, &p));
      out->message_id_data = (const uint8_t *)p; }
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi4_timers_value(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi4TimersValue *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    SAP_WIT_CHECK(sap_wit_read_message_envelope(region, cursor, &out->envelope));
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi5_dedupe_key(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi5DedupeKey *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->message_id_len));
    { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->message_id_len, &p));
      out->message_id_data = (const uint8_t *)p; }
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi5_dedupe_value(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi5DedupeValue *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag == SAP_WIT_TAG_BOOL_TRUE) out->accepted = 1;
      else if (tag == SAP_WIT_TAG_BOOL_FALSE) out->accepted = 0;
      else return ERR_TYPE; }
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->last_seen_ts));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->checksum_len));
    { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->checksum_len, &p));
      out->checksum_data = (const uint8_t *)p; }
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi6_dead_letter_key(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi6DeadLetterKey *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->worker));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->seq));
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi6_dead_letter_value(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi6DeadLetterValue *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    SAP_WIT_CHECK(sap_wit_read_message_envelope(region, cursor, &out->envelope));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->failure_code));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->attempts));
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_S64) return ERR_TYPE; }
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 8, &out->failed_at));
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_lease_state(const ThatchRegion *region, ThatchCursor *cursor, SapWitLeaseState *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_VARIANT) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 1, &out->case_tag));
    switch (out->case_tag) {
    case SAP_WIT_LEASE_STATE_QUEUED:
        break;
    case SAP_WIT_LEASE_STATE_LEASED:
        SAP_WIT_CHECK(sap_wit_read_lease_info(region, cursor, &out->val.leased));
        break;
    case SAP_WIT_LEASE_STATE_DONE:
        break;
    case SAP_WIT_LEASE_STATE_FAILED:
        { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
          if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
        SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->val.failed.len));
        { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->val.failed.len, &p));
          out->val.failed.data = (const uint8_t *)p; }
        break;
    default: return ERR_TYPE;
    }
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_dbi3_leases_value(const ThatchRegion *region, ThatchCursor *cursor, SapWitDbi3LeasesValue *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_RECORD) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    /* overflow-safe: reject if skip_len exceeds remaining bytes */
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    SAP_WIT_CHECK(sap_wit_read_lease_state(region, cursor, &out->state));
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

int sap_wit_read_tx_error(const ThatchRegion *region, ThatchCursor *cursor, SapWitTxError *out)
{
    { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
      if (tag != SAP_WIT_TAG_VARIANT) return ERR_TYPE; }
    uint32_t _skip_len;
    SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &_skip_len));
    uint32_t _remaining = thatch_region_used(region) - *cursor;
    if (_skip_len > _remaining) return ERR_RANGE;
    ThatchCursor _segment_end = *cursor + _skip_len;
    SAP_WIT_CHECK(thatch_read_data(region, cursor, 1, &out->case_tag));
    switch (out->case_tag) {
    case SAP_WIT_TX_ERROR_BUSY:
        break;
    case SAP_WIT_TX_ERROR_CONFLICT:
        break;
    case SAP_WIT_TX_ERROR_NOT_FOUND:
        break;
    case SAP_WIT_TX_ERROR_SCHEMA_MISMATCH:
        { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
          if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
        SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->val.schema_mismatch.len));
        { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->val.schema_mismatch.len, &p));
          out->val.schema_mismatch.data = (const uint8_t *)p; }
        break;
    case SAP_WIT_TX_ERROR_INTERNAL:
        { uint8_t tag; SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
          if (tag != SAP_WIT_TAG_BYTES && tag != SAP_WIT_TAG_STRING) return ERR_TYPE; }
        SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &out->val.internal.len));
        { const void *p; SAP_WIT_CHECK(thatch_read_ptr(region, cursor, out->val.internal.len, &p));
          out->val.internal.data = (const uint8_t *)p; }
        break;
    default: return ERR_TYPE;
    }
    if (*cursor != _segment_end) return ERR_TYPE;
    return ERR_OK;
}

/* ---- Universal skip ---- */

int sap_wit_skip_value(const ThatchRegion *region, ThatchCursor *cursor)
{
    uint8_t tag;
    SAP_WIT_CHECK(thatch_read_tag(region, cursor, &tag));
    switch (tag) {
    case SAP_WIT_TAG_S8:  case SAP_WIT_TAG_U8:
        return thatch_advance_cursor(region, cursor, 1);
    case SAP_WIT_TAG_S16: case SAP_WIT_TAG_U16:
        return thatch_advance_cursor(region, cursor, 2);
    case SAP_WIT_TAG_S32: case SAP_WIT_TAG_U32: case SAP_WIT_TAG_F32:
        return thatch_advance_cursor(region, cursor, 4);
    case SAP_WIT_TAG_S64: case SAP_WIT_TAG_U64: case SAP_WIT_TAG_F64:
        return thatch_advance_cursor(region, cursor, 8);
    case SAP_WIT_TAG_BOOL_TRUE: case SAP_WIT_TAG_BOOL_FALSE:
        return ERR_OK;
    case SAP_WIT_TAG_ENUM:
        return thatch_advance_cursor(region, cursor, 1);
    case SAP_WIT_TAG_FLAGS:
        return thatch_advance_cursor(region, cursor, 4);
    case SAP_WIT_TAG_STRING: case SAP_WIT_TAG_BYTES: {
        uint32_t len;
        SAP_WIT_CHECK(thatch_read_data(region, cursor, 4, &len));
        return thatch_advance_cursor(region, cursor, len);
    }
    case SAP_WIT_TAG_RECORD: case SAP_WIT_TAG_TUPLE:
    case SAP_WIT_TAG_LIST: case SAP_WIT_TAG_VARIANT: {
        uint32_t skip;
        SAP_WIT_CHECK(thatch_read_skip_len(region, cursor, &skip));
        return thatch_advance_cursor(region, cursor, skip);
    }
    case SAP_WIT_TAG_OPTION_SOME:
        return sap_wit_skip_value(region, cursor);
    case SAP_WIT_TAG_OPTION_NONE:
        return ERR_OK;
    case SAP_WIT_TAG_RESULT_OK: case SAP_WIT_TAG_RESULT_ERR:
        return sap_wit_skip_value(region, cursor);
    default: return ERR_TYPE;
    }
}

/* ---- DBI blob validators ---- */

int sap_wit_validate_dbi0_app_state_value(const void *data, uint32_t len)
{
    if (!data && !len) return 0;
    if (!data || !len) return -1;
    ThatchRegion view;
    if (thatch_region_init_readonly(&view, data, len) != ERR_OK) return -1;
    ThatchCursor cur = 0;
    SapWitDbi0AppStateValue scratch;
    memset(&scratch, 0, sizeof(scratch));
    int rc = sap_wit_read_dbi0_app_state_value(&view, &cur, &scratch);
    if (rc != ERR_OK) return -1;
    if (cur != len) return -1;
    return 0;
}

int sap_wit_validate_dbi1_inbox_value(const void *data, uint32_t len)
{
    if (!data && !len) return 0;
    if (!data || !len) return -1;
    ThatchRegion view;
    if (thatch_region_init_readonly(&view, data, len) != ERR_OK) return -1;
    ThatchCursor cur = 0;
    SapWitDbi1InboxValue scratch;
    memset(&scratch, 0, sizeof(scratch));
    int rc = sap_wit_read_dbi1_inbox_value(&view, &cur, &scratch);
    if (rc != ERR_OK) return -1;
    if (cur != len) return -1;
    return 0;
}

int sap_wit_validate_dbi2_outbox_value(const void *data, uint32_t len)
{
    if (!data && !len) return 0;
    if (!data || !len) return -1;
    ThatchRegion view;
    if (thatch_region_init_readonly(&view, data, len) != ERR_OK) return -1;
    ThatchCursor cur = 0;
    SapWitDbi2OutboxValue scratch;
    memset(&scratch, 0, sizeof(scratch));
    int rc = sap_wit_read_dbi2_outbox_value(&view, &cur, &scratch);
    if (rc != ERR_OK) return -1;
    if (cur != len) return -1;
    return 0;
}

int sap_wit_validate_dbi3_leases_value(const void *data, uint32_t len)
{
    if (!data && !len) return 0;
    if (!data || !len) return -1;
    ThatchRegion view;
    if (thatch_region_init_readonly(&view, data, len) != ERR_OK) return -1;
    ThatchCursor cur = 0;
    SapWitDbi3LeasesValue scratch;
    memset(&scratch, 0, sizeof(scratch));
    int rc = sap_wit_read_dbi3_leases_value(&view, &cur, &scratch);
    if (rc != ERR_OK) return -1;
    if (cur != len) return -1;
    return 0;
}

int sap_wit_validate_dbi4_timers_value(const void *data, uint32_t len)
{
    if (!data && !len) return 0;
    if (!data || !len) return -1;
    ThatchRegion view;
    if (thatch_region_init_readonly(&view, data, len) != ERR_OK) return -1;
    ThatchCursor cur = 0;
    SapWitDbi4TimersValue scratch;
    memset(&scratch, 0, sizeof(scratch));
    int rc = sap_wit_read_dbi4_timers_value(&view, &cur, &scratch);
    if (rc != ERR_OK) return -1;
    if (cur != len) return -1;
    return 0;
}

int sap_wit_validate_dbi5_dedupe_value(const void *data, uint32_t len)
{
    if (!data && !len) return 0;
    if (!data || !len) return -1;
    ThatchRegion view;
    if (thatch_region_init_readonly(&view, data, len) != ERR_OK) return -1;
    ThatchCursor cur = 0;
    SapWitDbi5DedupeValue scratch;
    memset(&scratch, 0, sizeof(scratch));
    int rc = sap_wit_read_dbi5_dedupe_value(&view, &cur, &scratch);
    if (rc != ERR_OK) return -1;
    if (cur != len) return -1;
    return 0;
}

int sap_wit_validate_dbi6_dead_letter_value(const void *data, uint32_t len)
{
    if (!data && !len) return 0;
    if (!data || !len) return -1;
    ThatchRegion view;
    if (thatch_region_init_readonly(&view, data, len) != ERR_OK) return -1;
    ThatchCursor cur = 0;
    SapWitDbi6DeadLetterValue scratch;
    memset(&scratch, 0, sizeof(scratch));
    int rc = sap_wit_read_dbi6_dead_letter_value(&view, &cur, &scratch);
    if (rc != ERR_OK) return -1;
    if (cur != len) return -1;
    return 0;
}

