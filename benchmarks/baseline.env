# Sapling benchmark regression baseline
#
# Baseline captured on 2026-02-21 from:
#   make bench-run BENCH_COUNT=100000 BENCH_ROUNDS=3
#
# Guardrail policy: fail only on large regressions.

BASELINE_COUNT=100000
BASELINE_ROUNDS=3
BASELINE_SPEEDUP=16.09
ALLOWED_REGRESSION_PCT=45

# Optional absolute thresholds (disabled by default; enable if CI hosts are stable):
# BASELINE_PUT_SEC=0.040340
# BASELINE_LOAD_SEC=0.002507
